void main() {
  // final Subject s = Subject(name: 'physics');
  // print(json.encode(s));
  // final t = Task(
  //     title: 'Do something',
  //     timeStart: '09:00',
  //     timeEnd: '10:30',
  //     color: Colors.black);
  // print(json.encode(t));
  // final subJson = json.encode(s);
  // print(Subject.fromJson(json.decode(subJson)).name);

  // final time = DateTime.now();
  // print((time.millisecondsSinceEpoch));
  // print(time.toUtc().millisecondsSinceEpoch);
  // // print(DateTime.from);
}
